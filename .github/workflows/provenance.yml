name: build-attest

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read
      attestations: write


    env:
     GH_TOKEN: ${{ github.TOKEN }}

    steps:
    # Artifacts download


     -  run: |
         sudo apt-fast -y update
         gh --version
         sudo apt-fast install -y gh --fix-missing
         gh --version
     # -  name: artifact attest verifyation
     #    ------
     #    run: gh attestation verify './example.txt' --repo milankomaj/data-livewallpaper