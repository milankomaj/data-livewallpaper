name: Unit tests

on:
  workflow_dispatch:
  push:
    branches:
      - '!master' 
  pull_request:
    branches:
      - '!master' 

jobs:
  testing:
    runs-on: ubuntu-latest
    steps:
    - name: Clone repository
      uses: actions/checkout@v2
    - name: Execute tests
      run: |
       chmod +x ./gradlew  
       ./gradlew test
